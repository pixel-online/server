# Pixel-Online-Server

> En cours de développement

## Démmarer le serveur

Pour lancer le serveur en local:

```bash
npm run start
```

Pour lancer le serveur en production:

```bash
npm run start:prod
```